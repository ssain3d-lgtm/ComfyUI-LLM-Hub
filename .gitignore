config.json
# 사용자가 손으로 고치는 프리셋 파일. 추적하면 git pull 이 사용자의 프리셋을
# 덮어쓴다. 배포용 원본은 system_prompts.example.json 이다.
system_prompts.json
_llmhub_tmp/
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# 토큰 파일은 절대 커밋하지 않는다 (내용 전체가 API 키다).
lm_studio_token.txt

# 프리셋 백업 파일. 저장소에 들어갈 물건이 아니다.
system_prompts.json.bak-*
